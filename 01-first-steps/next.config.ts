import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Para subir a GitHub/Vercel hay que comentar esta linea del Docker
  // output: "standalone",
};

export default nextConfig;
